import { FC } from 'react';

export const Home: FC = () => {
    return (
        <div className="m-8 flex justify-center text-center">
            Welcome to Signal Edge, this page is still under construction.
        </div>
    );
};
